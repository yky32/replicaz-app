# Appfile for fastlane — aligned with ClipVal / Triftly (KAI YIN YU team)

app_identifier("com.replicaz.replicaz")
apple_id(ENV["FASTLANE_USER"] || "wayneyu.dev@gmail.com")

team_id(ENV["FASTLANE_TEAM_ID"] || "3G34999H3A")

# App Store Connect team (optional if multi-team)
itc_team_id(ENV["FASTLANE_ITC_TEAM_ID"] || "128328295")
